# コード作成方針（開発用ec2まわり）

- 時刻などの設定値はUTCを直打ちする。JST→UTCの変換ロジックはコード（CloudFormation等）に持たせない
  - JST→UTC対応表はドキュメント側に書いておき、人が変換してパラメータに渡す
- 豪華な確認機能・便利機能は作らない（過剰なバリデーション、リッチな出力、あると便利程度の機能は追加しない）
- エラー処理は書かない
- コードは常にシンプルにし、人がすぐに理解できることを優先する
