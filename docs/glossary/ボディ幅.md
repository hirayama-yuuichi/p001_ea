# ボディ幅

ローソク足の「実体」部分の大きさ。始値と終値の差の絶対値。

## 計算例
```
始値: 2000 → 終値: 2005 → ボディ幅 = 5ドル
```

## 読み方
- ボディが大きい → 強い一方向の動き（トレンド発生）
- ボディが小さい → 迷い・方向感がない（エントリー見送り）

## コード例
```python
df["body"] = abs(df["close"] - df["open"])
df["body_ratio"] = df["body"] / (df["high"] - df["low"])  # レンジに対するボディの割合
```
