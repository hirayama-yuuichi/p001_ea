# 前足変化率

1本前のローソク足と比べて何%動いたかを示す特徴量。モメンタム（勢い）の基礎。

## 計算例
```
前足終値: 2000ドル → 今足終値: 2002ドル
変化率 = (2002 - 2000) / 2000 = +0.1%
```

## コード例
```python
df["ret_1"] = df["close"].pct_change(1)   # 1本前
df["ret_3"] = df["close"].pct_change(3)   # 3本前
```

## 使い方
連続して同方向に動いているか（モメンタム）や、急激な変化（ブレイク）の検出に使う。
