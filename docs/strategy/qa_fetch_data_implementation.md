# Q&A: データ取得スクリプト実装上の課題

仕様書作成時に検出された未決定事項。実装前に解答を得ておく。

---

## A. MT5データ取得スクリプト（fetch_mt5_data.py）

### Q1. 初回 vs 更新の判定ロジック
ファイルが存在するか確認して、処理を分岐させるか？
```
初回: 2021-01-01 ～ 2026-12-31 を5つのファイルに分割
更新: 最新ファイルの1年分だけ再取得
```
**質問**: どの条件でこれを判定する？

->パラメータで対象年を指定して実行(5回実行)
->パラメータが空なら今年


---

### Q2. 複数シンボル・複数時間足の取得順序
XAUUSD を M1, M5, M15, M60 で取得、その後 DXY を M5 で取得する際、
```
順序案1: シンボルごと（XAUUSD全時間足 → DXY）
順序案2: 時間足ごと（全シンボルのM1 → 全シンボルのM5 → ...）
順序案3: 時間足短い順（M1 → M5 → M15 → M60）
```
**質問**: どの順序が効率的か？
->順序案1
---

### Q3. エラーリトライ戦略
MT5接続時にエラー（timeout等）が発生した場合、
```
リトライ案1: N回リトライ → 失敗時は該当ファイルスキップ
リトライ案2: N回リトライ → 失敗時はスクリプト全体停止
```
**質問**: どのレベルで止めるべき？
->リトライ案1
---

### Q4. 部分的失敗時の Parquet 削除
スクリプト途中で失敗した場合、破損したParquetファイルはどう扱う？
```
案1: 自動削除（再実行時に再取得）
案2: 自動リネーム（_error サフィックス付け）
案3: 手動確認（ユーザーが削除判断）
```
**質問**: どれにする？
->案3
---

## B. Windows PowerShell から Python 実行

### Q5. conda 環境の activate
setup.ps1 で `fx59v2` 環境を作ったが、SSH 実行時に活性化されるか？
```
現在のセットアップ:
  C:\ProgramData\miniconda3\envs\fx59v2\python.exe -m ...

SSH経由実行時:
  ssh ... "python ml/fetch_mt5_data.py"
  → PATH が fx59v2 を指してるか？
```
**質問**: conda activate が必要か、それとも絶対パス指定で十分か？
fx59v2は使わない
win側で　p001_ea 配下に　.venv　requirements.txt　作った　localもそれに合わせる

---

### Q6. PowerShell ExecutionPolicy
Windows のセキュリティ設定で Python スクリプト実行をブロックしないか？
```
確認項目:
  - ExecutionPolicy の現在値
  - setup.ps1 で設定したか
  - SSH 経由実行で有効か
```
**質問**: 事前に何か設定が必要か？
->やってみる
---

### Q7. バックグラウンド実行
ローカルから SSH で実行する場合、長時間（5〜10分）かかるが、
```
案1: フォアグラウンド実行（終了まで待つ）
案2: バックグラウンド実行（`nohup ... &`）
案3: Windows Task Scheduler で定時実行
```
**質問**: ローカルからの SSH 実行では、どの方法が実用的か？
-＞案2
---

## C. EC2 側の環境

### Q8. MT5 の自動起動
MT5 は EC2 起動時に自動で立ち上がるか？ または毎回手動起動が必要か？
```
確認項目:
  - Windows スタートアップフォルダに MT5 ショートカット
  - タスクスケジューラで自動起動設定
  - 現在の状態
```
**質問**: 自動起動設定をすべきか？
->毎回手動起動


---

### Q9. SSH サーバーの確認
EC2 Windows に SSH サーバーが本当に起動しているか？
```
テスト方法:
  - telnet ec2-ip 22
  - ssh -v で詳細ログ確認
  - Windows PowerShell の `Enable-PSRemoting`
```
**質問**: SSH が動いているか確認する方法は？

->?

---

## D. ローカル → EC2 の接続

### Q10. EC2 IP アドレスの管理
EC2 は起動するたびに新しい IP アドレスを取得するが、
```
案1: 毎回 AWS コンソール → IP 確認 → ~/.ssh/config 更新
案2: Elastic IP を設定（固定IP）
案3: EC2 tags で名前をつけ、CLI で自動解決
```
**質問**: 実運用では Elastic IP を取得すべきか？

start-ec2-and-rdp.shで起動時に取得するので、
start-ec2-and-rdp.shを改造して.envに記載する案はどう？

---

### Q11. SSH キーの管理
ec2_key.pem の置き場所とアクセス権限は？
```
案1: ~/.ssh/ec2_key.pem （標準）
案2: プロジェクトフォルダ内（infra/keys/）
案3: パスワードマネージャー連携
```
**質問**: どこに保存・管理するべきか？
-＞今は、~/.ssh/ec2_key.pemだが、どこに保存が良い？
---

## E. データ管理

### Q12. Parquet ファイルのバージョン管理
複数の Parquet ファイルが存在する場合、古いバージョンをどう扱う？
```
案1: 上書き（最新だけ残す）
案2: バージョン付け（XAUUSD_M5_20260101_20261231_v2.parquet）
案3: 日付で履歴保持（XAUUSD_M5_20260101_20261231_20260405.parquet）
```
**質問**: データ管理戦略は？
案1
---

## 優先度順

**実装前に必ず決める:**
- Q1 初回 vs 更新判定
- Q5 conda 環境の活性化
- Q6 ExecutionPolicy

**実装中に詰める:**
- Q2 取得順序
- Q3 エラーリトライ
- Q4 部分失敗時の処理

**本番運用で決める:**
- Q7 バックグラウンド実行
- Q8 MT5 自動起動
- Q10 Elastic IP
- Q12 ファイルバージョン管理

**検証・確認:**
- Q9 SSH サーバー確認
- Q11 SSH キー管理
